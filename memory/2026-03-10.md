# 2026-03-10 - INST002 Config Migration

## Task Completed: #428 - routes.js Config Migration

**Status**: ✅ Complete

### What Was Done

**1. Updated backend/config.js**
- Added tilde expansion for `paths.agentWorkspaces`
- Added tilde expansion for `paths.openclawWorkspace`
- Now expands all workspace paths at config load time

**2. Simplified backend/routes.js**
- Removed hardcoded clawd path fallbacks
- Simplified `getAgentWorkspace()` function
- Now uses pre-expanded paths from config
- Removed redundant tilde expansion logic

### Changes Made

**backend/config.js** (lines ~163-179):
```javascript
// Resolve paths.agentWorkspaces
if (config.paths && config.paths.agentWorkspaces) {
  for (const agentId of Object.keys(config.paths.agentWorkspaces)) {
    const workspacePath = config.paths.agentWorkspaces[agentId];
    if (workspacePath.startsWith('~')) {
      config.paths.agentWorkspaces[agentId] = workspacePath.replace('~', os.homedir());
    }
  }
}

// Resolve paths.openclawWorkspace
if (config.paths && config.paths.openclawWorkspace) {
  if (config.paths.openclawWorkspace.startsWith('~')) {
    config.paths.openclawWorkspace = config.paths.openclawWorkspace.replace('~', os.homedir());
  }
}
```

**backend/routes.js** (getAgentWorkspace function):
- Before: 27 lines with hardcoded fallbacks and tilde expansion
- After: 12 lines using pre-expanded config paths
- Removed: All `clawd` hardcoded paths
- Removed: Redundant tilde expansion (now in config loader)

### Testing

✅ Config loader correctly expands tildes:
```javascript
{
  "main": "C:\\Users\\Samuel/.openclaw/workspace",
  "athena": "C:\\Users\\Samuel/clawd/agent-dashboard",
  "hephaestus": "C:\\Users\\Samuel/clawd/agent-dashboard"
}
```

### Impact

- **No hardcoded paths**: All workspace paths now come from config
- **Instance-per-project**: Each project can have its own workspace mappings
- **Cleaner code**: Removed 15+ lines of fallback logic
- **Centralized config**: All path resolution happens in config.js

### Note

⚠️ Backend needs restart to load updated config loader. The changes will take effect on next server restart.

---

## INST002 Status

**Completed**: 1/6 tasks (Hephaestus's assigned work)

**Remaining Tasks** (assigned to Athena):
- #424: ResearchDrawer.tsx migration
- #425: AgentPanel.tsx migration
- #426: AgentStream.tsx migration
- #427: Billboard.tsx migration
- #429: Verify all hardcoded URLs removed

Hephaestus's work on INST002 is complete. Standing by for new tasks.

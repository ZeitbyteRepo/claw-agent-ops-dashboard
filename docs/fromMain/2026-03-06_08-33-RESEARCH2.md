# Dispatch #105: [RESEARCH2] Research Drawer: Pin/Unpin + Archive

**Plan:** #82
**Created:** 2026-03-06 08:33 CST

---

## Objective

Enhance the research drawer with:
1. Pin/unpin functionality for research items
2. Archive footer drawer for old/archived research

---

## Tasks

| ID | Title | Agent | Notes |
|----|-------|-------|-------|
| #398 | Backend: Add pinned column to research_docs table | hephaestus | Add boolean pinned column, default false |
| #399 | Backend: Pin/unpin API endpoints | hephaestus | PATCH /api/research/:id/pin, PATCH /api/research/:id/unpin |
| #400 | Backend: Archived research API endpoint | hephaestus | GET /api/research/archived |
| #401 | Frontend: Pin/unpin button on research items | athena | Pin icon, toggle pinned state on click |
| #402 | Frontend: Sort pinned items to top | athena | Pinned items appear first in drawer |
| #403 | Frontend: Archive footer in research drawer | athena | Collapsible footer showing archived research count |

---

## Technical Context

### Backend (Hephaestus)
- `research_docs` table needs `pinned` boolean column (default false)
- New endpoints:
  - `PATCH /api/research/:id/pin` - set pinned = true
  - `PATCH /api/research/:id/unpin` - set pinned = false
  - `GET /api/research/archived` - return archived research docs
- Modify `GET /api/research` to sort by pinned DESC, then created_at DESC

### Frontend (Athena)
- Add pin icon (📌) button to each research item
- Pinned items show at top of drawer with visual indicator
- Archive footer at bottom of drawer:
  - Collapsed: "📦 X archived research ▶"
  - Expanded: List of archived items

---

## API Reference

- Claim task: `PATCH /api/tasks/:id` with `{ status: "in_progress" }`
- Complete task: `PATCH /api/tasks/:id` with `{ status: "done" }`
- Get dispatch: `GET /api/dispatches/105`

---

## Verification

After completing:
1. Pin button appears on research items
2. Clicking pin moves item to top of drawer
3. Archive footer shows at bottom
4. Archive footer expands to show archived research

# Dispatch #107: [INST001-RESEARCH] Research Instance-Per-Project Architecture

**Target Agent:** Athena
**Plan:** #83
**Created:** 2026-03-06 15:33 CST

---

## Objective

Research the best approach for making the agent-dashboard instance-per-project ready. Document findings in the research drawer.

---

## Task

| ID | Title | Agent | Notes |
|----|-------|-------|-------|
| #404 | Research instance-per-project architecture | athena | Document findings in research drawer |

---

## Research Questions

1. **Database Path Configuration**
   - Current: Hardcoded `data/dashboard.db`
   - Options: Environment variable? Config file? CLI argument?

2. **API Base URL Handling**
   - Current: Hardcoded `localhost:3001`
   - How should different instances communicate?

3. **Per-Instance State**
   - SSE clients are in-memory
   - Message emitter is shared
   - How to isolate per instance?

4. **Port Management**
   - How to handle multiple instances on different ports?
   - Auto-assign or configure?

5. **Existing Patterns**
   - Are there similar projects with good patterns?
   - PM2 ecosystem? Docker compose?

---

## What We Already Know

From earlier codebase review:
- `db.js` - SQLite with hardcoded path
- `routes.js` - 900+ lines with all handlers
- `openclaw-watcher.js` - Session file watcher
- OpenClaw path already uses `os.homedir()` - flexible

---

## Expected Output

1. **Research document in the research drawer** with:
   - Analysis of each touchpoint
   - Recommended approach for each
   - Code examples if helpful
   - Trade-offs

2. **Create research doc via API:**
   ```
   POST /api/research
   {
     "title": "Instance-Per-Project Architecture Research",
     "content": "... your findings ..."
   }
   ```

---

## API Reference

- Claim task: `PATCH /api/tasks/404` with `{ status: "in_progress" }`
- Complete task: `PATCH /api/tasks/404` with `{ status: "done" }`
- Create research: `POST /api/research`
- Get dispatch: `GET /api/dispatches/107`

---

## Success Criteria

- Research appears in the research drawer (📚 button in header)
- Document covers all 5 research questions
- Clear recommendations provided

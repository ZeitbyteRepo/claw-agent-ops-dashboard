# Dispatch #82: [DASH2] Dashboard UX Improvements - Phase 2

**To:** Hephaestus (Backend) + Athena (Frontend)
**From:** Mr. Claw
**Date:** 2026-03-03 19:28
**Status:** Dispatched
**Dispatch ID:** 82
**Plan ID:** 66

---

## Mission

Complete the Dashboard UX improvements with full traceability to parent ideas via code badges.

---

## Tasks by Agent

### Hephaestus (Backend) - 2 Tasks

| Task ID | Code | Title |
|---------|------|-------|
| #286 | AGNO001 | Backend: PLANNING tasks are agent-agnostic |
| #288 | DISP001 | Backend: API returns TODO tasks grouped by dispatch_id |

### Athena (Frontend) - 7 Tasks

| Task ID | Code | Title |
|---------|------|-------|
| #287 | AGNO001 | Frontend: PLANNING column shows unassigned tasks |
| #289 | DISP001 | Frontend: Dispatch wrapper for TODO column |
| #290 | DPOP001 | Frontend: Dispatch popup showing tasks + details |
| #291 | PDET001 | Frontend: Plan details in planning popup |
| #292 | NOTE001 | Frontend: Notes on ideas for review/edit in popup |
| #293 | ARCH001 | Frontend: Archive drawer for all columns |
| #294 | DPER001 | Frontend: Dispatches persist with finished tasks, retire to archive |

---

## Task Details

### AGNO001: PLANNING Tasks Agent-Agnostic

**Backend (#286):**
- Tasks in PLANNING status should NOT have `agent_id`
- Only assign `agent_id` when moving to TODO (at dispatch phase)

**Frontend (#287):**
- PLANNING column shows tasks without agent assignment
- No agent badge displayed for planning tasks

---

### DISP001: Dispatch Wrapper

**Backend (#288):**
- API endpoint returns TODO tasks grouped by `dispatch_id`
- New endpoint: `GET /api/tasks?status=todo&grouped=dispatch`

**Frontend (#289):**
- TODO column groups tasks under dispatch wrapper (like Plan wrapper)
- Click dispatch wrapper to see all tasks in that dispatch

---

### DPOP001: Dispatch Popup

**Frontend (#290):**
- Create DispatchPopup component (similar to PlanPopup)
- Shows dispatch details + all assigned tasks
- Shows which tasks are done vs pending

---

### PDET001: Plan Details Popup

**Frontend (#291):**
- When clicking task in PLANNING, show plan description
- Fetch plan details via `GET /api/plans/:id`
- Display in TaskPopup

---

### NOTE001: Notes on Ideas

**Frontend (#292):**
- Add notes textarea to idea cards
- Notes icon on card if notes exist
- Editable in TaskPopup

---

### ARCH001: Archive Drawer

**Frontend (#293):**
- Bottom slide-up drawer component
- Tabs: Ideas, Plans, Dispatches, Done tasks
- Restore button on archived items

---

### DPER001: Dispatches Persist

**Frontend (#294):**
- Dispatches stay visible after tasks complete
- Show completed tasks separately (collapsed)
- "Archive" button when all tasks done

---

## API Endpoints

Claim task:
```
PATCH http://localhost:3001/api/tasks/{id}
{"status": "in_progress"}
```

Complete task:
```
PATCH http://localhost:3001/api/tasks/{id}
{"status": "done"}
```

---

## Workflow

**Hephaestus:** Start with #286, then #288
**Athena:** Start with #291 (plan details), then work through remaining frontend tasks

Each task has a `code` field linking to its parent idea. Check the idea for full context if needed.

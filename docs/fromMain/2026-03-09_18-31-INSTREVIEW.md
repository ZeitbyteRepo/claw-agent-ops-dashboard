# Dispatch #111: [INSTREVIEW] Instance-Per-Project Implementation Review

**Plan:** #86
**Created:** 2026-03-09 18:31 CST
**Target:** Athena

---

## Objective

Conduct a comprehensive review and audit of the instance-per-project architecture implementation. Produce a detailed research report that Samuel can review.

---

## Tasks

| ID | Title | Notes |
|----|-------|-------|
| #419 | Audit Configuration System | Review dashboard.config.json, config.js, env overrides, path resolution |
| #420 | Audit Backend Integration | Verify server.js, db.js, routes.js, watcher use config |
| #421 | Audit Frontend Integration | Verify config loader, api.ts, App.tsx, etc. use config |
| #422 | Identify Gaps & Create Recommendations | Find hardcoded values, suggest improvements |
| #423 | Write Research Report | Compile findings into research document |

---

## Context

We've implemented a configuration system for the agent dashboard to support isolated instances per project. This review validates the implementation and identifies gaps.

### Files to Review

**Configuration:**
- `dashboard.config.json` - Main config file
- `backend/config.js` - Config loader

**Backend:**
- `backend/server.js` - Server entry point
- `backend/db.js` - Database module
- `backend/routes.js` - API routes
- `backend/openclaw-watcher.js` - File watcher

**Frontend:**
- `frontend/src/lib/config.ts` - Frontend config loader
- `frontend/src/lib/api.ts` - API client
- `frontend/src/App.tsx` - Main app component
- `frontend/src/components/AgentStream.tsx` - Agent stream component
- `frontend/index.html` - HTML entry point

---

## Deliverable

A research document saved to the research drawer (`POST /api/research`) containing:

1. **Implementation Audit** - ✅/❌ checklist for each component
2. **Gaps & Issues** - Any hardcoded values, missing options
3. **Recommendations** - Improvements for multi-instance deployment
4. **Spin-up Guide** - Step-by-step process for creating new instances

---

## API Reference

- Claim task: `PATCH /api/tasks/:id` with `{ status: "in_progress" }`
- Complete task: `PATCH /api/tasks/:id` with `{ status: "done" }`
- Get dispatch: `GET http://localhost:3001/api/dispatches/111`
- Save research: `POST http://localhost:3001/api/research`
